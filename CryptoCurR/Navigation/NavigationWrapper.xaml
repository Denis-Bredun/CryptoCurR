<Window x:Class="CryptoCurR.Navigation.NavigationWrapper"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptoCurR.Navigation"
        xmlns:vm="clr-namespace:CryptoCurR.ViewModels"
        xmlns:views="clr-namespace:CryptoCurR.Views"
        mc:Ignorable="d"
        Style="{StaticResource MainWindow}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:MainPageViewModel}">
            <views:MainPage/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CoinDetailsViewModel}">
            <views:CoinDetailsPage />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CurrencyConverterViewModel}">
            <views:CurrencyConverterPage />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>

</Window>
