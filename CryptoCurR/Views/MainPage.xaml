<UserControl x:Class="CryptoCurR.Views.MainPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptoCurR.Views"
        xmlns:lottie="clr-namespace:LottieSharp.WPF;assembly=LottieSharp"
        mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2E2E2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                                <Setter Property="BorderBrush" Value="#888"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3A3A3A"/>
                                <Setter Property="BorderBrush" Value="#AAA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="#666"/>
                                <Setter Property="BorderBrush" Value="#222"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="#1B1B1B"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="true"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#333333"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderTextBlock" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>
    <Grid>
        <Grid Margin="15">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 15" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBox Width="610"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Search" Command="{Binding SearchCoinsCommand}" />
                <Button Content="Reset" Command="{Binding ResetCommand}" />
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0 0 0 8" Background="#2A2A2A">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Icon" Grid.Column="0" Style="{StaticResource HeaderTextBlock}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Name" Grid.Column="1" Style="{StaticResource HeaderTextBlock}"/>
                    <TextBlock Text="Symbol" Grid.Column="2" Style="{StaticResource HeaderTextBlock}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Price" Grid.Column="3" Style="{StaticResource HeaderTextBlock}" HorizontalAlignment="Right"/>
                    <TextBlock Text="24h Change" Grid.Column="4" Style="{StaticResource HeaderTextBlock}" HorizontalAlignment="Right"/>
                    <TextBlock Text="Market Cap" Grid.Column="5" Style="{StaticResource HeaderTextBlock}" HorizontalAlignment="Right"/>
                    <TextBlock Text="Rank" Grid.Column="6" Style="{StaticResource HeaderTextBlock}" HorizontalAlignment="Center"/>
                </Grid>

                    <ListView ItemsSource="{Binding Coins}" Grid.Row="1" SelectedItem="{Binding SelectedCoin, Mode=TwoWay}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction Command="{Binding NavigateToCoinDetailsCommand}" 
                                                  CommandParameter="{Binding SelectedCoin}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <Image Grid.Column="0" Width="40" Height="40" Stretch="Uniform" Source="{Binding Image}" />

                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />

                            <TextBlock Grid.Column="2" Text="{Binding Symbol}" HorizontalAlignment="Center" VerticalAlignment="Center" />

                            <TextBlock Grid.Column="3" Text="{Binding CurrentPrice, StringFormat=C}" HorizontalAlignment="Right" VerticalAlignment="Center" />

                            <TextBlock Grid.Column="4" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock.Text>
                                      <Binding Path="PriceChangePercentage24h" StringFormat="{}{0:F2}%"/>
                                </TextBlock.Text>
                                <TextBlock.Foreground>
                                      <Binding Path="PriceChangePercentage24h" Converter="{StaticResource PriceChangeToBrushConverter}"/>
                                </TextBlock.Foreground>
                            </TextBlock>

                            <TextBlock Grid.Column="5" Text="{Binding MarketCap, StringFormat=N0}" HorizontalAlignment="Right" VerticalAlignment="Center" />

                            <TextBlock Grid.Column="6" Text="{Binding MarketCapRank}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </Grid>

            <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Button Content="&lt; Previous" Command="{Binding PreviousPageCommand}" />
                <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="10,0" FontWeight="Bold" FontSize="14"/>
                <Button Content="Next &gt;" Command="{Binding NextPageCommand}" />

                <Separator Width="30" Visibility="Collapsed"/>

                <Button Content="+ Items" Command="{Binding IncreasePageSizeCommand}" />
                <Button Content="- Items" Command="{Binding DecreasePageSizeCommand}" />
                <TextBlock Text="{Binding CoinsPerPage}" VerticalAlignment="Center" Margin="10,0" FontWeight="Bold" FontSize="14"/>
            </StackPanel>

        </Grid>

        <Border Background="#80000000"
        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Panel.ZIndex="100">
            <lottie:LottieAnimationView
        FileName="Assets\\loading.json"
        RepeatCount="-1"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Width="100"
        Height="100"
        AutoPlay="True" />
        </Border>


    </Grid>

</UserControl>
