<UserControl x:Class="CryptoCurR.Views.CoinDetailsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CryptoCurR.Views"
             xmlns:vm="clr-namespace:CryptoCurR.ViewModels"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:lottie="clr-namespace:LottieSharp.WPF;assembly=LottieSharp"
             mc:Ignorable="d">

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="300"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Style="{StaticResource CoinDetailsHeader}">
                <Button Content="← Back"
                        Command="{Binding GoBackCommand}"
                        Style="{StaticResource BackButton}"/>
                <Image Style="{StaticResource CoinDetailsImage}"
                       Source="{Binding CoinDetail.Image.Large}"/>
                <StackPanel Style="{StaticResource CoinDetailsInfo}">
                    <TextBlock Style="{StaticResource CoinNameText}"
                               Text="{Binding CoinDetail.Name}" />
                    <TextBlock Style="{StaticResource CoinSymbolText}"
                               Text="{Binding CoinDetail.Symbol}" />
                </StackPanel>
            </StackPanel>

            <UniformGrid Grid.Row="1" 
                         Style="{StaticResource MarketDataGrid}">
                <StackPanel Style="{StaticResource MarketDataItem}">
                    <TextBlock Style="{StaticResource MarketDataLabel}"
                               Text="Current Price:" />
                    <TextBlock Style="{StaticResource MarketDataValue}"
                               Text="{Binding CoinDetail.MarketData.CurrentPrice.usd, StringFormat='{}{0:C}'}" />
                </StackPanel>
                <StackPanel Style="{StaticResource MarketDataItem}">
                    <TextBlock Style="{StaticResource MarketDataLabel}"
                               Text="Market Cap:" />
                    <TextBlock Style="{StaticResource MarketDataValue}"
                               Text="{Binding CoinDetail.MarketData.MarketCap.usd, StringFormat='{}{0:C}'}" />
                </StackPanel>
                <StackPanel Style="{StaticResource MarketDataItem}">
                    <TextBlock Style="{StaticResource MarketDataLabel}"
                               Text="24h Volume:" />
                    <TextBlock Style="{StaticResource MarketDataValue}"
                               Text="{Binding CoinDetail.MarketData.TotalVolume.usd, StringFormat='{}{0:C}'}" />
                </StackPanel>
                <StackPanel Style="{StaticResource MarketDataItem}">
                    <TextBlock Style="{StaticResource MarketDataLabel}"
                               Text="24h Change:" />
                    <TextBlock Style="{StaticResource MarketDataValue}"
                               Text="{Binding CoinDetail.MarketData.PriceChangePercentage24h, StringFormat='{}{0:F2}%'}"
                               Foreground="{Binding CoinDetail.MarketData.PriceChangePercentage24h, Converter={StaticResource PriceChangeToBrushConverter}}" />
                </StackPanel>
                <StackPanel Style="{StaticResource MarketDataItem}">
                    <TextBlock Style="{StaticResource MarketDataLabel}"
                               Text="7d Change:" />
                    <TextBlock Style="{StaticResource MarketDataValue}"
                               Text="{Binding CoinDetail.MarketData.PriceChangePercentage7d, StringFormat='{}{0:F2}%'}"
                               Foreground="{Binding CoinDetail.MarketData.PriceChangePercentage7d, Converter={StaticResource PriceChangeToBrushConverter}}" />
                </StackPanel>
                <StackPanel Style="{StaticResource MarketDataItem}">
                    <TextBlock Style="{StaticResource MarketDataLabel}"
                               Text="Circulating Supply:" />
                    <TextBlock Style="{StaticResource MarketDataValue}"
                               Text="{Binding CoinDetail.MarketData.CirculatingSupply, StringFormat='{}{0:N0}'}" />
                </StackPanel>
            </UniformGrid>

            <StackPanel Grid.Row="2" 
                        Style="{StaticResource ChartControlsPanel}">
                <TextBlock Style="{StaticResource ChartTypeLabel}"/>
                <RadioButton Content="Line" 
                             IsChecked="{Binding IsLineChart}" 
                             GroupName="ChartType" 
                             Style="{StaticResource ChartTypeRadioButton}"/>
                <RadioButton Content="Candlestick" 
                             IsChecked="{Binding IsCandlestickChart, Mode=OneWay}" 
                             GroupName="ChartType" 
                             Style="{StaticResource ChartTypeRadioButton}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" 
                        Style="{StaticResource TimeframePanel}">
                <TextBlock Style="{StaticResource TimeframeLabel}"/>
                <ItemsControl ItemsSource="{Binding AvailableTimeframes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" 
                                    Style="{StaticResource TimeframeButton}"
                                    Command="{Binding DataContext.ChangeTimeframeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <Button.Background>
                                    <MultiBinding Converter="{StaticResource TimeframeToBrushMultiConverter}">
                                        <Binding Path="DataContext.SelectedTimeframe" 
                                                 RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                        <Binding />
                                    </MultiBinding>
                                </Button.Background>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </StackPanel>

            <lvc:CartesianChart Grid.Row="3"
                                Visibility="{Binding IsLineChart, Converter={StaticResource BoolToVisConverter}}"
                                Series="{Binding MarketChart, Converter={StaticResource MarketChartConverter}}"
                                Zoom="X"
                                LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Time" 
                              LabelFormatter="{Binding DateLabelFormatter}" />
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Price (USD)" />
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>

            <lvc:CartesianChart Grid.Row="3"
                                Visibility="{Binding IsCandlestickChart, Converter={StaticResource BoolToVisConverter}}"
                                Series="{Binding OhlcCandles, Converter={StaticResource OhlcSeriesConverter}}"
                                Zoom="X"
                                LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Time" 
                              LabelFormatter="{Binding OhlcLabelFormatter}" 
                              LabelsRotation="15"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Price (USD)" />
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>

            <GroupBox Grid.Row="4" 
                      Style="{StaticResource MarketsGroupBox}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" 
                                Style="{StaticResource MarketsHeader}">
                        <TextBlock Text="Market" 
                                   Width="150" 
                                   Style="{StaticResource MarketsHeaderText}"/>
                        <TextBlock Text="Base" 
                                   Width="50" 
                                   Style="{StaticResource MarketsHeaderText}"/>
                        <TextBlock Text="Target" 
                                   Width="50" 
                                   Style="{StaticResource MarketsHeaderText}"/>
                        <TextBlock Text="Last" 
                                   Width="100" 
                                   Style="{StaticResource MarketsHeaderText}"/>
                        <TextBlock Text="Volume" 
                                   Width="100" 
                                   Style="{StaticResource MarketsHeaderText}"/>
                        <TextBlock Text="" 
                                   Width="40"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" 
                                  Style="{StaticResource MarketsScrollViewer}">
                        <ItemsControl ItemsSource="{Binding CoinTickers.Tickers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource MarketItemBorder}">
                                        <StackPanel Style="{StaticResource MarketItemPanel}">
                                            <TextBlock Text="{Binding Market.Name}" 
                                                       Width="150" 
                                                       Style="{StaticResource MarketItemText}"/>
                                            <TextBlock Text="{Binding Base}" 
                                                       Width="50" 
                                                       Style="{StaticResource MarketItemText}"/>
                                            <TextBlock Text="{Binding Target}" 
                                                       Width="50" 
                                                       Style="{StaticResource MarketItemText}"/>
                                            <TextBlock Text="{Binding Last, StringFormat='{}{0:F2}'}" 
                                                       Width="100" 
                                                       Style="{StaticResource MarketItemText}"/>
                                            <TextBlock Text="{Binding Volume, StringFormat='{}{0:N0}'}" 
                                                       Width="100" 
                                                       Style="{StaticResource MarketItemText}"/>
                                            <TextBlock Width="40" 
                                                       Style="{StaticResource MarketOpenLink}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="MouseLeftButtonDown">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.OpenTradeUrlCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                              CommandParameter="{Binding TradeUrl}" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </GroupBox>
        </Grid>

        <Border Style="{StaticResource LoadingOverlayBorder}"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <lottie:LottieAnimationView
                FileName="Assets\\loading.json"
                Style="{StaticResource LoadingAnimation}"
                AutoPlay="True"/>
        </Border>
    </Grid>
</UserControl>
